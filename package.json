{
  "name": "e-commerce-store",
  "version": "1.0.0",
  "description": "A modern e-commerce store with Trieve integration",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "build": "cd client && npm run build && cd ../server && npm run build",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install",
    "convert-csv": "node convert_csv_to_trieve_batches.js",
    "upload-trieve": "node upload_to_trieve.js",
    "process-data": "npm run convert-csv && npm run upload-trieve"
  },
  "keywords": ["e-commerce", "react", "nodejs", "trieve", "ai"],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "axios": "^1.6.2",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}